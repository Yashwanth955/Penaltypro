version: 3
tunnels:
  dev:
    proto: http
    addr: 5173
    subdomain: penaltypro-dev  # Optional: use a specific subdomain
    # auth: "username:password"  # Optional: add basic auth
    # hosted_headers:
    #   - "X-Custom-Header: value"
    region: us  # Options: us, eu, au, ap, sa, jp, in
# authtoken: YOUR_NGROK_AUTH_TOKEN  # Add your ngrok auth token here or set NGROK_AUTHTOKEN env var
